{
    "name": "laravel-ready-vitepress",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "docs:dev": "vitepress dev resources/docs",
        "docs:build": "vitepress build resources/docs",
        "docs:preview": "vitepress preview resources/docs",
        "build": "npm run docs:build && npm run copy-dist",
        "copy-dist": "cp -r resources/docs/.vitepress/dist/* public/docs/"
    },
    "devDependencies": {
        "vitepress": "^1.0.0",
        "vue": "^3.3.0"
    }
}
